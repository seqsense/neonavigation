<?xml version="1.0"?>
<package format="3">
  <name>costmap_cspace</name>
  <version>0.18.3</version>
  <description>3-dof configuration space costmap package</description>

  <maintainer email="atsushi.w@ieee.org">Atsushi Watanabe</maintainer>
  <license>BSD</license>
  <author email="atsushi.w@ieee.org">Atsushi Watanabe</author>

  <buildtool_depend condition="$ROS_VERSION == 1">catkin</buildtool_depend>
  <buildtool_depend condition="$ROS_VERSION == 2">ament_cmake</buildtool_depend>
  <buildtool_depend condition="$ROS_VERSION == 2">ament_cmake_auto</buildtool_depend>

  <!-- ROS 1 interface layer (roscpp nodes) and ROS 1-only helpers. -->
  <depend condition="$ROS_VERSION == 1">roscpp</depend>
  <!-- XmlRpc parameter parsing lives in the ROS 1 interface layer only. -->
  <depend condition="$ROS_VERSION == 1">xmlrpcpp</depend>
  <!-- Provides the rclcpp-style shim used by the hybrid logic library on ROS 1. -->
  <depend condition="$ROS_VERSION == 1">sq_ros1_rclcpp_compat</depend>

  <!-- ROS 2 interface layer uses the real rclcpp (nodes added in a later step). -->
  <depend condition="$ROS_VERSION == 2">rclcpp</depend>

  <depend>geometry_msgs</depend>
  <depend>laser_geometry</depend>
  <depend>nav_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>tf2_geometry_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_sensor_msgs</depend>

  <depend>costmap_cspace_msgs</depend>
  <depend>neonavigation_common</depend>

  <test_depend condition="$ROS_VERSION == 1">roslint</test_depend>
  <test_depend condition="$ROS_VERSION == 1">rostest</test_depend>
  <test_depend condition="$ROS_VERSION == 2">ament_cmake_gtest</test_depend>

  <export>
    <build_type condition="$ROS_VERSION == 1">catkin</build_type>
    <build_type condition="$ROS_VERSION == 2">ament_cmake</build_type>
  </export>
</package>
