# Source dependencies not available from the Alpine ROS package repository,
# shared by the ROS 1 and ROS 2 CI jobs.
#
# neonavigation_msgs is released, but only in its pre-hybrid form, which does
# not emit the ROS 2-style headers these packages include; sq_ros1_rclcpp_compat
# is not released at all. Both are therefore built from source here.
#
# sq_ros1_rclcpp_compat is ROS 1-only, so the ROS 2 job leaves it out of its
# manifest-files: it is checked out but never built there.
- git:
    local-name: sq_ros1_rclcpp_compat
    uri: https://github.com/seqsense/sq_ros1_rclcpp_compat.git
    version: main
- git:
    local-name: neonavigation_msgs
    uri: https://github.com/seqsense/neonavigation_msgs.git
    version: master
